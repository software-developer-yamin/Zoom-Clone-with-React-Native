import { StyleSheet, Text, View } from "react-native";

const SearchBar = () => {
  return (
    <View>
      <Text>SearchBar</Text>
    </View>
  );
};

export default SearchBar;

const styles = StyleSheet.create({});
